import React from 'react';
import ChatPage from './ChatPage';

// 如果后续还需要加入其他页面，可以在这里添加路由，目前直接展示聊天页面
const App = () => {
  return (
    <div>
      <ChatPage />
    </div>
  );
};

export default App;
